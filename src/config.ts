export const dbConnectionConfig = {
  user: 'postgres',
  host: 'localhost',
  database: 'limon',
  password: 'root',
  port: Number(5432),
}

export const ROOT_ROUTE = `/api`;

export const FILE_STORAGE_DIR_NAME = "static";

export const TABLES = {
  items: 'items'
}